{
  "name": "gifgrep",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "make gifgrep",
    "gifgrep": "make gifgrep",
    "fmt": "gofumpt -w .",
    "lint": "golangci-lint run",
    "snap": "node scripts/ghostty-web-snap.mjs",
    "build": "go build ./...",
    "test": "go test ./...",
    "check": "go vet ./..."
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
